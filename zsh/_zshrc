# Path to your oh-my-zsh configuration.
ZSH=$HOME/.oh-my-zsh

# Set name of the theme to load.
# Look in ~/.oh-my-zsh/themes/
# Optionally, if you set this to "random", it'll load a random theme each
# time that oh-my-zsh is loaded.
ZSH_THEME="bureau"
#ZSH_THEME="agnoster"
#ZSH_THEME="random"
# good ones:
#   bureau

# Example aliases
# alias zshconfig="mate ~/.zshrc"
# alias ohmyzsh="mate ~/.oh-my-zsh"

# Set to this to use case-sensitive completion
# CASE_SENSITIVE="true"

# Comment this out to disable bi-weekly auto-update checks
# DISABLE_AUTO_UPDATE="true"

# Uncomment to change how many often would you like to wait before auto-updates occur? (in days)
# export UPDATE_ZSH_DAYS=13

# Uncomment following line if you want to disable colors in ls
# DISABLE_LS_COLORS="true"

# Uncomment following line if you want to disable autosetting terminal title.
# DISABLE_AUTO_TITLE="true"

# Uncomment following line if you want red dots to be displayed while waiting for completion
# COMPLETION_WAITING_DOTS="true"

# Which plugins would you like to load? (plugins can be found in ~/.oh-my-zsh/plugins/*)
# Custom plugins may be added to ~/.oh-my-zsh/custom/plugins/
# Example format: plugins=(rails git textmate ruby lighthouse)
plugins=(git colorize rvm)

source $ZSH/oh-my-zsh.sh
# Customize to your needs...
alias x='exit'

alias rm='rm -i'
alias cp='cp -i'
alias mv='mv -i'

alias aptitude='sudo aptitude'

#vim
alias vs='sudo vim'
alias vo='vim -O' 
alias vp='vim -p'
alias vx='vim -R'
alias vvi='vim'
alias vd='vimdiff'

#git
alias gsubp='git submodule foreach git pull origin master'

#memory usage
alias psm='ps aux | awk '"'"'{print $6, "\t", $11}'"'"' | sort -n'
alias psm-watch="watch 'ps aux | awk '"'"'"'"'"'"'{print "'$6'", "'"'""'\t'""'"'", "'$11'"}'"'"'"'"'"'"' | sort -rn'"

# VPN
#alias vicstart='sudo /etc/openvpn/vicstart.sh'
#alias vicstop='sudo /etc/openvpn/vicstop.sh'
alias src-sfcati='sftp dsmith@cati2'
alias src-rdv='remmina -c ~/.remmina/vts1.remmina &'
alias src-rdv2='remmina -c ~/.remmina/vts2.remmina &'
alias src-rdp='remmina -c ~/.remmina/pts1.remmina &'
alias src-rdp2='remmina -c ~/.remmina/pts2.remmina &'
alias src-rdpsas='remmina -c ~/.remmina/psas1.remmina &'
alias src-rdsql='remmina -c ~/.remmina/sql.remmina &'
alias src-mnz='sudo mount //vdc1/zdrive/ /srcentre/zdrive -t cifs -o credentials=/home/danny/.srcentre,uid=danny,gid=danny'
alias src-mno='sudo mount //pdc1/odrive/ /srcentre/odrive -t cifs -o credentials=/home/danny/.srcentre,uid=danny,gid=danny'
alias src-mnr='sudo mount //cati2/projects/ /srcentre/rdrive -t cifs -o credentials=/home/danny/.srcentre,uid=danny,gid=danny'
alias src-mnmydoc='sudo mount //vdc1/userprofiles/Documents/dsmith/My\ Documents /srcentre/mydocs -t cifs -o credentials=/home/danny/.srcentre,uid=danny,gid=danny'
alias src-umnt='sudo umount /srcentre/*'

alias src-mmon='xfreerdp -u dsmith -d srcentre -K -f --plugin cliprdr'


export PATH=$HOME/local/bin/:$HOME/.cabal/bin/:$PATH

tvim () { tmux new-session "vim --servername VIM $*" }

#[[ -s ~/.autojump/etc/profile.d/autojump.sh ]] && . ~/.autojump/etc/profile.d/autojump.sh
#autoload -U compinit && compinit
